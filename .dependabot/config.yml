version: 1
update_configs:
  - package_manager: "submodules"
    directory: "./"
    update_schedule: "daily"
    allowed_updates:
      - match:
          dependency_name: "7-TimPushkin"
      - match:
          dependency_name: "7-smmac72"
      - match:
          dependency_name: "7-Ramazanov-Amir"
      - match:
          dependency_name: "7-PyRayman"
      - match:
          dependency_name: "7-AShlegel"
    ignored_updates:
      - match:
          dependency_name: "7-sorter-utility"
